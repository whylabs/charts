{{- if .Values.cache.enable }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-cache
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    app: {{ .Release.Name }}-cache
{{- end }}
