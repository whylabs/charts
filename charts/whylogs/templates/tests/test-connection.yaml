apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whylogs.fullname" . }}-test-connection"
  labels:
    {{- include "whylogs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whylogs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
